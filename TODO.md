1. Convert the CHECKLIST.md to checklist.json- subsected by planet
2. Update the checklist so each item is actionable;  e.g. 
3. Init a vite vanilla webapp that presents the checklist where the user can mark-off different items as they are completed;  user can also rename a session and upload/download a checklist to track their progress
   a. the checklist viewer/editor should handle generic json lists/sub-lists so that- as the default checklist evolves- older checklists are still usable (without us having to version anything)
5. Add a GH action to bulid/deploy the webapp via `gh-pages` package
